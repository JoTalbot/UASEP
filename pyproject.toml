[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uasep"
version = "3.1.0"
description = "Universal Autonomous Engineering & Self-Maintenance Protocol reference runtime"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
