[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uasep"
version = "3.1.1"
description = "Universal Autonomous Engineering & Self-Maintenance Protocol reference runtime"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
uasep = "runtime.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
