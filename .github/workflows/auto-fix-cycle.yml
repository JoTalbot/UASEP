name: UASEP Auto Fix Cycle

# Legacy compatibility controller. The canonical repair entrypoint is
# UASEP Auto Repair Orchestrator; this workflow remains manually runnable.
on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read
  actions: read

jobs:
  prepare-repair:
    runs-on: ubuntu-latest
    steps:
      - name: Report legacy controller status
        run: |
          echo "UASEP Auto Fix Cycle is retained as a manual compatibility entrypoint."
          echo "Canonical automated repair flow: UASEP Auto Repair Orchestrator"
