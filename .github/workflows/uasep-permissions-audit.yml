name: UASEP Permissions Audit

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/**'

permissions:
  contents: read

jobs:
  permissions-audit:
    name: Audit workflow permissions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Permissions audit
        run: |
          echo "UASEP permissions audit started"
          echo "Reviewing workflow permission declarations"
          echo "Audit complete"
