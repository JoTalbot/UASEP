name: UASEP Runtime Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  runtime-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Runtime validation
        run: echo "UASEP runtime validation started"
